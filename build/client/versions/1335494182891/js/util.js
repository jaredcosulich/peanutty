// Generated by CoffeeScript 1.9.3
(function() {
  window.escapeRegExp = function(str) {
    return str.replace(/[.*+?#^$|(){},\[\]\-\s\\]/g, '\\$&');
  };

  window.jsonify = function(ob) {
    var name, result, value;
    if (ob == null) {
      ob = {};
    }
    result = {};
    for (name in ob) {
      value = ob[name];
      result[name] = JSON.stringify(value);
    }
    return result;
  };

  window.zeroPad = function(n, width) {
    var i, j, ref, s, zeros;
    s = n.toString();
    zeros = width - s.length;
    for (i = j = 0, ref = zeros; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
      s = '0' + s;
    }
    return s;
  };

  String.prototype.startsWith = function(str) {
    return this.substr(0, str.length) === str;
  };

  String.prototype.endsWith = function(str) {
    return this.substr(this.length - str.length, str.length) === str;
  };

  String.prototype.capitalize = function() {
    return this.charAt(0).toUpperCase() + this.slice(1).toLowerCase();
  };

  String.prototype.toTitleCase = function() {
    var i, j, ref, str, words;
    str = "";
    words = this.split(" ");
    for (i = j = 0, ref = words.length; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
      str += (i != null ? i : {
        " ": ""
      }) + words[i].capitalize();
    }
    return str;
  };

  Date.prototype.longMonths = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];

  Date.prototype.shortMonths = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];

  Date.prototype.longDays = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];

  Date.prototype.shortDays = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];

  Date.prototype.strftime = function(format) {
    var day, hours, minutes, month, seconds, weekday, year;
    year = this.getFullYear();
    month = this.getMonth();
    day = this.getDate();
    weekday = this.getDay();
    hours = this.getHours();
    minutes = this.getMinutes();
    seconds = this.getSeconds();
    return format.replace(/\%([aAbBcdDHiImMpSwyY])/g, (function(_this) {
      return function(part) {
        var ref, ref1, ref2;
        switch (part[1]) {
          case 'a':
            return _this.shortDays[weekday];
          case 'A':
            return _this.longDays[weekday];
          case 'b':
            return _this.shortMonths[month];
          case 'B':
            return _this.longMonths[month];
          case 'c':
            return _this.toString();
          case 'd':
            return day;
          case 'D':
            return zeroPad(day, 2);
          case 'H':
            return zeroPad(hours, 2);
          case 'i':
            return (ref = hours === 12 || hours === 0) != null ? ref : {
              12: (hours + 12) % 12
            };
          case 'I':
            return zeroPad((ref1 = hours === 12 || hours === 0) != null ? ref1 : {
              12: (hours + 12) % 12
            }, 2);
          case 'm':
            return zeroPad(month + 1, 2);
          case 'M':
            return zeroPad(minutes, 2);
          case 'p':
            return (ref2 = hours > 11) != null ? ref2 : {
              "PM": "AM"
            };
          case 'S':
            return zeroPad(seconds, 2);
          case 'w':
            return weekday;
          case 'y':
            return zeroPad(year % 100);
          case 'Y':
            return year;
        }
      };
    })(this));
  };

}).call(this);
